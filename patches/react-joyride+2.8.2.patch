diff --git a/node_modules/react-joyride/dist/index.mjs b/node_modules/react-joyride/dist/index.mjs
index 9371e9c..9f2e0db 100644
--- a/node_modules/react-joyride/dist/index.mjs
+++ b/node_modules/react-joyride/dist/index.mjs
@@ -1,6 +1,6 @@
 import React, { useState, useEffect, useRef, useCallback, useMemo, Fragment } from 'react';
 import { jsx, jsxs } from 'react/jsx-runtime';
-import ReactDOM from 'react-dom';
+import { createPortal } from 'react-dom';
 import PropTypes from 'prop-types';
 import { computePosition, flip, shift, offset, arrow, inline, autoUpdate, hide } from '@floating-ui/dom';
 import { size } from '@floating-ui/react-dom';
@@ -1020,7 +1020,7 @@
 
   useEffect(() => {
     return () => {
-      ReactDOM.unmountComponentAtNode(portal.current);
+      // No direct equivalent needed for createPortal, React handles unmounting
     };
   }, []);
   useEffect(() => {
@@ -1029,8 +1029,7 @@
       return;
     }
 
-    // @ts-ignore
-    ReactDOM.unstable_renderSubtreeIntoContainer(instance, element, portal.current);
+    // unstable_renderSubtreeIntoContainer is removed. createPortal handles this.
   }, [instance, element, portal]);
   useEffect(() => {
     if (!container) {
@@ -1046,7 +1045,7 @@
     return;
   }
 
-  return portal.current;
+  return createPortal(element, portal.current);
 };
 
 const propTypes$5 = {
